<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("../partials/head")%>
        <title>Create new comment</title>
    </head>
    <body>
        <%- include("../partials/navbar")%>
        <main>
            <div class="container">
                <div class="columns">
                    <div class="column is-half is-offset-one-quarter">
                        <h1 class="title">Create a new comment:</h1>
                        <form action="/comments" class="mb-5">
                            <div class="field"><button class="button is-danger">View all comments</button></div>
                        </form>
                        <form action="/comments" method="post">
                            <div class="field">
                                <label for="username" class="label">Username:</label>
                                <input type="text" class="input" id="username" name="username" required />
                            </div>
                            <div class="field">
                                <label for="comment" class="label">Comment:</label>
                                <input type="textarea" class="input" id="comment" name="comment" required />
                            </div>
                            <div class="field"><button class="button is-primary" type="submit">Submit</button></div>
                        </form>
                    </div>
                </div>
            </div>
        </main>
        <%- include("../partials/footer.ejs")%>
    </body>
</html>
